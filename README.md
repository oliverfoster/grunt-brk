# grunt-brk
Run `grunt` with `--inspect-brk`  

### Installation:
`$ npm install -g grunt-brk`  

### Usage:
Instead of  
`$ grunt command`  
  
Run  
`$ grunt-brk command`  
  
Visit  
[chrome://inspect/#devices](chrome://inspect/#devices)
